import { InnerPage } from "../components/InnerPage";

export default function RevetementPage() {
  return <InnerPage eyebrow="Revêtements" title="Revêtements sols & murs" text="Pose de revêtements, remise au propre et finitions intérieures pour moderniser vos pièces avec un rendu soigné." />;
}
